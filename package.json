{
  "name": "yuno-webdesign-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "yuno webdesign ポートフォリオサイト",
  "scripts": {
    "sass": "sass scss/main.scss css/style.css --style=expanded --source-map --watch",
    "build": "sass scss/main.scss css/style.css --style=compressed --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
